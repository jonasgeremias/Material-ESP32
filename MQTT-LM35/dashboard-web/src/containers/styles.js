import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    container: {
        paddingTop: 40,
        paddingLeft: '10vw',
        paddingRight: '10vw'
    },
    grid: {
        marginTop: 50,
        flexGrow: 1
    }
}))